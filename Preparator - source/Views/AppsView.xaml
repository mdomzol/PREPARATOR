<UserControl x:Class="Preparator.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Preparator.ViewModels">

    <UserControl.DataContext>
        <vm:AppsViewModel/>
    </UserControl.DataContext>

    <Grid Background="#1e1e1e">
        <StackPanel Margin="20">

            <TextBlock Text="Applications"
                       Foreground="White"
                       FontSize="24"
                       Margin="0,0,0,20"/>

            <ItemsControl ItemsSource="{Binding Apps}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Border Margin="10"
                                Padding="15"
                                Width="200"
                                CornerRadius="10"
                                BorderThickness="2">

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#2d2d30"/>
                                    <Setter Property="BorderBrush" Value="#2d2d30"/>

                                    <Style.Triggers>
                                        
                                        <!-- SELECTED -->
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="#3a8dde"/>

                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#3a8dde"
                                                                      BlurRadius="20"
                                                                      ShadowDepth="0"
                                                                      Opacity="0.8"/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        
                                        <!-- HOVER -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#555"/>
                                        </Trigger>
                                        
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Button Command="{Binding DataContext.ToggleAppCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent"
                                    BorderThickness="0">

                                <TextBlock Text="{Binding Name}"
                                           Foreground="White"
                                           FontSize="14"
                                           HorizontalAlignment="Center"/>
                            </Button>

                        </Border>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Install Selected"
                    Command="{Binding InstallCommand}"
                    Margin="0,20,0,0"
                    Height="40"/>
        </StackPanel>
    </Grid>
</UserControl>